<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <title><%= article.title %></title>
  <link rel="icon" href="/media/PP.svg" />
  <meta name="description" content="<%=  article.description %>" />
  <meta name="author" content="<%=  article.author %>" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <link href="/styles/article.css" rel="stylesheet">
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0-alpha3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-KK94CHFLLe+nY2dmCWGMq91rCGa5gtU4mk92HdvYe+M/SXH301p5ILy+dN9+nJOZ" crossorigin="anonymous" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap-icons@1.10.5/font/bootstrap-icons.css" />
  <title>Pirate Party UK (PPUK) - <%= article.title %></title>
</head>

<body itemscope itemtype="http://schema.org/Article">
  <!-- would like to have prams in the url for selfpost -->

  <!-- BS alert box -->

  <%- include('partials/header') %>
  <!-- <div class="container">
    <script>
      // let get url params
      const element = document.getElementById('selfpost');
      const params = new URLSearchParams(window.location.search);
      console.log(params.has('selfpost')); // true
      if (params.has('selfpost')) {
        element.style.display = 'block';
        console.log('selfpost');
      } else {
        element.style.display = 'none';
        console.log('not selfpost');
      }
    </script>
    <div class="alert alert-primary" role="alert" id="selfpost">
      <strong>info</strong> Hay you just click on a self-post from a social media platform. this mean how ever post this is a member of the party and not the party it self. if you want to see the party's post please go to the <a href="/articles">articles</a> page.
    </div> -->
    <header>
      <h1 class="entry-title" itemprop="headline"><%= article.title %></h1>
      <div class="summary text-muted" itemprop="description">
        <%= article.summary %>
      </div>
      <div class="dateline">
        by
        <span itemprop="author" itemscope itemtype="http://schema.org/Person">
          <span itemprop="name"><%= article.name %></span>
        </span>
        <p>
          <time itemprop="datePublished" datetime="<%= article.datePublished %>">
            published <%= article.datePublished.toDateString() %>
          </time>
    </header>
    <p></p>
    <div class="entry-content" itemprop="articleBody">
      <%- article.htmlContent %>
    </div>
  </div>
  <%- include('partials/footer') %>
</body>

</html>